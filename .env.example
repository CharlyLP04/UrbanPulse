# Servidor
BACKEND_PORT=

# Base de datos (PostgreSQL)
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# Frontend
# URL del backend que consume el frontend
NEXT_PUBLIC_API_URL=

# Entorno
NODE_ENV=development
# Cambiar a 'production' en despliegue para activar secure: true en cookies

# Autenticación
JWT_SECRET=
JWT_REFRESH_SECRET=

# Recuperación de contraseña
RESET_TOKEN_EXPIRY_MINUTES=30
APP_URL=http://localhost:3000